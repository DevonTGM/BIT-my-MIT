<div id="bitmymit-mount"></div>
<script>
    (() => {
        const mount = document.currentScript.previousElementSibling;
        if (!mount) return;

        const links = {
            quiz: "https://lexue.bit.edu.cn/mod/hvp/view.php?id=514901",
            pdf: "https://lexue.bit.edu.cn/pluginfile.php/761257/mod_resource/content/1/AI%20Overview%20Study%20Guide.pdf?forcedownload=1",
            files: "https://lexue.bit.edu.cn/mod/folder/view.php?id=512418",
            github: "#"
        };

        const members = "Mikhael · Andrey · Kenrich · Dmitrii · Arina · Alina";

        const root = document.createElement("div");
        root.className = "hwc";

        root.innerHTML = `
  <div class="floating-bg"></div>

  <div class="hero">
    <span class="badge">AI Overview</span>
    <h1 class="title">(15) BITmyMIT💦</h1>
    <p class="sub">
      If we can learn AI, you can too.
    </p>

    <div class="tags">
      <span class="tag pastel-blue">Study Guide</span>
      <span class="tag pastel-pink">Slides</span>
      <span class="tag pastel-yellow">Files</span>
      <span class="tag pastel-purple">Quiz</span>
    </div>

    <div class="cta">
      <a class="btn primary" href="${links.quiz}" target="_blank">Start Quiz</a>
      <a class="btn soft" href="${links.pdf}" target="_blank">Open Study Guide (PDF)</a>
    </div>

    <div class="by">Made by ${members}</div>
  </div>

  <div class="grid">

    <a class="card" href="${links.quiz}" target="_blank">
      <span class="icon">🧠</span>
      <h3 class="card-title">Interactive Quiz</h3>
      <p class="card-desc">Learn by exploring at your own pace.</p>
      <span class="card-link">Start →</span>
    </a>

    <a class="card" href="${links.pdf}" target="_blank">
      <span class="icon">📘</span>
      <h3 class="card-title">Study Guide</h3>
      <p class="card-desc">Warm explanations & soft visuals.</p>
      <span class="card-link">Open →</span>
    </a>

    <a class="card" href="${links.files}" target="_blank">
      <span class="icon">📂</span>
      <h3 class="card-title">Slides & Resources</h3>
      <p class="card-desc">Everything organized neatly.</p>
      <span class="card-link">View →</span>
    </a>

    <div class="card muted">
      <span class="icon">💻</span>
      <h3 class="card-title">GitHub Repository</h3>
      <p class="card-desc">Coming soon — stay tuned!</p>
      <span class="card-link">...</span>
    </div>

    <div class="card muted">
      <span class="icon">🎬</span>
      <h3 class="card-title">Video</h3>
      <p class="card-desc">Soft animated explainer in progress.</p>
      <span class="card-link">...</span>
    </div>

  </div>
  `;

        mount.appendChild(root);

        const style = document.createElement("style");
        style.textContent = `
  .hwc {
    --radius: 16px;
    font-family: 'Inter', 'Segoe UI', sans-serif;
    max-width: 980px;
    margin: 0 auto;
    position: relative;
    overflow: hidden;
  }

  /* Soft floating pastel animation */
  .floating-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 20% 80%, rgba(255,190,220,.35), transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(190,210,255,.35), transparent 50%),
      radial-gradient(circle at 60% 70%, rgba(255,230,200,.35), transparent 50%);
    animation: float 12s ease-in-out infinite alternate;
    filter: blur(32px);
    z-index: -1;
  }

  @keyframes float {
    0% { transform: translateY(0px) scale(1); }
    100% { transform: translateY(-40px) scale(1.08); }
  }

  .hero {
    background: rgba(255,255,255,0.68);
    backdrop-filter: blur(14px);
    border-radius: calc(var(--radius) + 4px);
    padding: 28px;
    border: 1px solid rgba(0,0,0,.05);
    margin-bottom: 28px;
  }

  .badge {
    background: #eceaff;
    color: #6a63b6;
    padding: 6px 12px;
    border-radius: 999px;
    font-size: 13px;
  }

  .title { font-size: 38px; font-weight: 800; margin: 4px 0 6px; color:#333; }
  .sub { color:#6b6b6b; margin-bottom: 14px; font-size: 18px; font-weight: 500; }

  .tags { display:flex; flex-wrap:wrap; gap:8px; margin-bottom:16px; }
  .tag { padding:5px 10px; border-radius: 999px; font-size:12px; font-weight:600; border:1px solid rgba(0,0,0,.07); }
  .pastel-blue { background:#edf7ff; color:#3b6bb5; }
  .pastel-pink { background:#ffeef4; color:#b54b7d; }
  .pastel-yellow { background:#fff8dc; color:#8a6b00; }
  .pastel-purple { background:#f3edff; color:#6b4bb5; }

  .cta { display:flex; flex-wrap:wrap; gap:10px; }

  .btn {
    padding:10px 18px; border-radius:12px; font-weight:600;
    text-decoration:none; transition:.25s; display:inline-flex; align-items:center;
  }
  .btn.primary {
    background: linear-gradient(135deg, #b38cff, #ff9ee4);
    color:white; border:none;
  }
  .btn.primary:hover { transform: translateY(-2px); filter:brightness(1.08); }

  .btn.soft {
    background:#ffffff; border:1px solid rgba(0,0,0,.1); color:#6b6b96;
  }
  .btn.soft:hover { background:#fafaff; }

  .by { margin-top:10px; font-size:13px; color:#888; }

  .grid { display:grid; gap:18px; grid-template-columns:repeat(auto-fit,minmax(240px,1fr)); }

  .card {
    background: rgba(255,255,255,0.72);
    backdrop-filter: blur(10px);
    border:1px solid rgba(0,0,0,.08);
    border-radius:var(--radius);
    padding:18px;
    box-shadow:0 6px 18px rgba(0,0,0,.04);
    transition:.25s;
    text-decoration:none;
    color:#333;
    display:flex;
    flex-direction:column;
    min-height:150px;
  }
  .card:hover { transform:translateY(-3px); box-shadow:0 14px 30px rgba(0,0,0,.08); }
  .card.muted { opacity:.6; pointer-events:none; }

  .icon {
  font-size: 20px;
  width: 46px;
  height: 46px;
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 6px;
  background: rgba(255,255,255,0.6);
  backdrop-filter: blur(6px);
  border: 1px solid rgba(255,255,255,0.8);
  box-shadow: 0 4px 12px rgba(180, 160, 255, .18);
  transition: 0.25s ease;
}
.card:hover .icon {
  transform: translateY(-2px) scale(1.05);
  box-shadow: 0 8px 18px rgba(180, 160, 255, .28);
}

  .card-title { font-weight:700; margin:2px 0; }
  .card-desc { color:#777; font-size:14px; margin-bottom:6px;}
  .card-link { margin-top:auto; font-weight:600; color:#a366e8; }
  `;
        mount.appendChild(style);

    })();
</script>
